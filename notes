tri douteux sur export (exemple: lol, loll, lol2, lol3)

probleme en cas de | avec un builtin en premiere, (exemple "env | grep TERM")

probleme valgrind en cas de "export" apres avoir creer une nouvelle variable

cas des '/' qui devrait renvoyer is a directory

syntaxe error &, (), 

Echo cas 89 a Gerer

Tests <= 100 : OK

backslash a gerer? exemple (\$HOME = $HOME)?

cas 129 a verifier (echo $hola*) maffiche * doit il rien afficher?

cas 150 echo $"" affiche $ au lieu de \n pourquoi? echo $ est sense affihcher $
	echo $"HOME" maffiche $HOME au lieu de HOME
	echo $''HOME maffiche $HOME au lieu de HOME

	Si $ avant cotes, dollar disparait

cat <<lol""
	n'expand pas

si variable entre cote, la value devient un seul arguments
